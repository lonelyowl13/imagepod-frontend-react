# Base URL of the ImagePod API. No trailing slash.
# When unset, the dev server proxies /api to http://localhost:8000.
# For production or a remote backend, set this (e.g. https://api.example.com).
VITE_API_URL=http://localhost:8000
